<h1>My tasks</h1>
<%= link_to 'Create new task', tasks_new_path %>
<% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %>
    <p><%= task.details %></p>
    <p><%= task.completed %></p>
  </li>
<% end %>
